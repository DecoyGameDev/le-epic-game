//reset ping
var ping = 0;

//alert if ping is high
if (ping = 90){
    console.write("Your ping is over 90!")

}
else{
    console.write("Ping is currently stable.")
}

function pingdisplay(params) {
   const PingDisplayer
   var color = green;
   var text = ping_placeholder;

   var height = 6;
   var width = 5;

   while (ping = 90 < 90) {
       var color = green;
   }
   while (ping = 90 > 90) {
       var color = red;
   }
   if (ping = 80000 > 80000) {
       var text = ping
   }
   else{
       console.write("Your ping is too high, kicking out!")
       throw new Error();
   }
}